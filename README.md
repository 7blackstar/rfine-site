# RFINE Site

Static RFINE website foundation for GitHub Pages.

## Edit later

- Update copy, links, tools, release notes, and FAQ in `data/site.json`.
- Replace placeholder screenshots in `assets/screenshots/`.
- Open `index.html` locally to preview.

## Deploy

Push this folder to a GitHub repository and enable GitHub Pages from the root of the `main` branch.
